# Day 10a Demo — Python dependencies
# TravelBot: FastAPI backend (SSE streaming) + Gradio frontend
#
# Install:  pip install -r requirements.txt
#
# Run backend:  uvicorn backend:app --reload --port 8000
# Run frontend: python frontend.py

# ── ADK + LLM ────────────────────────────────────────────────────────────────
google-adk==2.1.0
litellm==1.86.2
python-dotenv==1.2.2

# ── Backend ───────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# ── Frontend ──────────────────────────────────────────────────────────────────
gradio>=4.0.0
httpx>=0.25.0
