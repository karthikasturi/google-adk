# Copy this file to .env and fill in your values.
# The demo loads it automatically via python-dotenv.

# ── OpenRouter API key (used for both chat and embedding models) ─────────
OPENROUTER_API_KEY=your-openrouter-api-key-here
