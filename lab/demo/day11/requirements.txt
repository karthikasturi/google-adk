# Day 11 — Real-time voice TravelBot
#
# One OpenRouter API key covers STT, the agent LLM, and TTS — no local models.
#
# Install:  pip install -r requirements.txt
# Run:      python pipeline.py            (live mic loop)
#           python selftest.py            (mic-less latency benchmark)
#
# System audio libs (Debian/Ubuntu):  sudo apt-get install -y libportaudio2

# ── Agent (Google ADK, same as previous days) ────────────────────────────────
google-adk==2.1.0
litellm==1.86.2
python-dotenv==1.2.2

# ── OpenRouter audio calls + audio capture/playback + VAD ───────────────────
httpx>=0.27                 # STT/TTS HTTP calls to OpenRouter
sounddevice>=0.4.6          # microphone capture + speaker playback
webrtcvad-wheels>=2.0.10    # voice-activity detection (prebuilt wheels)
numpy>=1.24
