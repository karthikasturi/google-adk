# Day 08 Demo — Python dependencies
# TravelBot agent calling MCP tool servers (built with mcp.server.fastmcp)
# over stdio via Google ADK's MCPToolset.
#
# Install:  pip install -r requirements.txt
# Run:      python demo.py

google-adk[mcp]==2.1.0
litellm==1.86.2
python-dotenv==1.2.2
